{
  "name": "@proappstore/backend",
  "version": "0.0.0",
  "description": "Cloudflare Worker for @proappstore/sdk — Stripe webhooks, subscriptions, license keys.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@proappstore/compliance": "workspace:*",
    "hono": "^4.6.0",
    "web-push": "^3.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/web-push": "^3.6.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
